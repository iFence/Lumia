# Changelog

本文件用于维护每个版本对外发布的更新说明。

编写约定：
- 每个版本使用 `## v版本号` 作为标题，例如 `## v0.1.3`
- 标题下方直接填写会展示在应用更新弹窗中的 Markdown 内容
- 使用 `---` 或下一个版本标题结束当前版本说明

## v0.2.0

### ✨ 新增功能
- **RAW 格式预览插件**：支持安装可选的 RAW 预览插件，浏览相机 RAW 格式（如 CR2、NEF、ARW 等）照片。
- **在地图上查看拍摄位置**：状态栏新增位置入口，点击即可在系统地图中打开照片的 GPS 拍摄地点。
- **图片信息面板快捷操作**：信息浮层新增一键复制和关闭按钮，查看和分享 EXIF 信息更方便。
- **格式关联重新设计**：设置中的图片格式关联全面升级，管理文件类型更直观灵活。
- **右键菜单文件操作**：在浏览区域右键即可复制或查看 EXIF 信息、复制文件路径或打开所在位置、将当前图片删除到回收站，右键菜单还支持一键切换全屏。

### 🚀 优化改进
- **窗口标题更实用**：标题栏直接显示当前图片在文件夹中的位置（如"第 3/20 张"），无需打开侧栏即可了解浏览进度。
- **大图浏览更干净**：修复了超大图片缩放时可能出现的细线（接缝伪影），画面更完美。
- **菜单交互更自然**：编辑菜单和缩放菜单紧贴触发按钮弹出，操作路径更短更直观。
- **空状态提示更贴心**：拖入图片的引导提示会根据你的操作系统显示对应的快捷键（macOS 显示 ⌘ 键，Windows 显示 Ctrl 键）。

---

## v0.1.5

### ✨ 新增功能
- 签名插件包安装：支持从文件安装官方签名的 `.lumiaplugin` 插件包，安装前验证签名、平台、架构和完整性。
- 启动时更新检查：应用启动时自动查询 GitHub Release 是否有新版本，在设置-关于面板中显示更新提示。

### 🚀 优化改进
- 完善 README 文档：添加版本号、下载量、许可证、平台等 GitHub 徽章，新增中文版 README。
- 更新检查支持跳过特定版本，避免重复打扰。

---

## v0.1.3

### ✨ 新增功能
- 快速 JPEG 预览缓存：为相邻文件添加预加载机制，提升浏览流畅度。
- 丰富的 EXIF 元数据提取与展示：支持更多 EXIF 标签的解析和显示。

### 🚀 优化改进
- 修复 CI 发布工作流权限问题。

---

## v0.1.2

### ✨ 新增功能
- 首个公开预览版本：基础图片浏览、缩放、平移、旋转。
- 文件夹导航、EXIF 信息、文件关联与快捷键自定义。

### 🚀 优化改进
- 启动速度与内存占用针对日常浏览场景进行了调优。

---
